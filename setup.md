To run this project, install Python 3.10 or later, then run:

python main.py

This will print the current list of tasks to the console. Use add_task() from utils.py to add new tasks programmatically.
